<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation_home"
    app:startDestination="@id/home_fragment">

    <fragment
        android:id="@+id/home_fragment"
        android:name="com.mikolove.allmight.views.home.HomeFragment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_home">
        <action
            android:id="@+id/action_home_fragment_to_chooseWorkoutFragment"
            app:destination="@id/choose_workout_fragment" />
    </fragment>
    <fragment
        android:id="@+id/choose_workout_fragment"
        android:name="com.mikolove.allmight.views.home.choose.ChooseWorkoutFragment"
        android:label="@string/workout_title"
        tools:layout="@layout/fragment_choose_workout" />

</navigation>